#include <stdio.h>
#include <string.h>

const char *byte_to_binary(int x)
{
    static char b[9];
    b[0] = '\0';

    int z;
    for (z = 128; z > 0; z >>= 1)
    {
        strcat(b, ((x & z) == z) ? "1" : "0");
    }

    return b;
}


int main()
{
int i=0;

for ( i=1; i<=26; i++ )
{
	printf("%c - %s\n", (char)i+'a', byte_to_binary(i));
}
return 0;
}
